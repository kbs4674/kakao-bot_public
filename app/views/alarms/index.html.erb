<% if alert %>
  <%= alert %>
<% else %>
  <h2>사자후 구독 처리결과</h2>
  <div>방 이름 : <%= @room_search.title %></div>
  <div>알람 상태 : <%= @room_search.is_agree %></div>
  <div>구분 : <%= @status %></div>
<% end %>